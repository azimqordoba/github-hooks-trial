const hello = function (a) {
  console.log('Hello ! how are you ? ');
};

hello();

const showMessage = () => {
  const message = 'I love ESLint';
  for (let i = 0; i < 5; i++) {
    console.log(message);
  }
};
showMessage();
