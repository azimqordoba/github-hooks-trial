export const square = (value) => value * value;

export const multiply = (arr) => arr.reduce((result, val) => result * val, 1);

export const addAll = (arr) => arr.reduce((result, val) => result + val, 1);

function azim () { 
  const a;
}
